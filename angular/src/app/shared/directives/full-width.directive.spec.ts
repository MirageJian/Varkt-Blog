import { FullWidthDirective } from './styles.directive';

describe('FullWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new FullWidthDirective();
    expect(directive).toBeTruthy();
  });
});
