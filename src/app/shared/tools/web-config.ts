export class WebConfig {
  private static  readonly widthOption = ['(max-width: 600px)', '(max-width: 960px)'];
  public static readonly mobileWidth = WebConfig.widthOption[1];
}

class WidthOption{
}
